"""
Top-level package for the quality_backend application.

This file exists to ensure the `src.*` modules are importable regardless of how the
application is launched (e.g., `uvicorn src.api.main:app`).
"""
